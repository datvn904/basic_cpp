#include <bits/stdc++.h>
using namespace std;

unsigned int t;//không đeo thẻ
unsigned int n;//nói chuyện
unsigned int m;//đi trễ

int main() {
  cin >> t >> n >> m;
  cout << (t * 3) + (n * 2) + (m * 5);
  return 0;
}
